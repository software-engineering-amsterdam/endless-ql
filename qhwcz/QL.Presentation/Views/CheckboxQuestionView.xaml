<UserControl x:Class="Presentation.Views.CheckboxQuestionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.Views"
             xmlns:converters="clr-namespace:Presentation.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <converters:BooleanNegationConverter x:Key="BooleanNegationConverter"/>
        <Style x:Key="QLSStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{Binding Style.Foreground}"/>
            <Setter Property="FontSize" Value="{Binding Style.FontSize}"/>
            <Setter Property="FontFamily" Value="{Binding Style.FontFamily}"/>
            <Setter Property="Width" Value="{Binding Style.Width}"/>
        </Style>
    </UserControl.Resources>
    <CheckBox IsChecked="{Binding Value}" Style="{StaticResource QLSStyle}" IsEnabled="{Binding Path=IsEvaluated, Converter={StaticResource BooleanNegationConverter}}" Content="{Binding Description}"/>

</UserControl>
