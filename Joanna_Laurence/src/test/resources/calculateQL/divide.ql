//AssertDisplayedQuestions=1
form taxOfficeExample {

    if (15 / 3 == 5) {
      "Did you wait to longq?"
        waitedToLong1: boolean = true
    }

    if (15 / 3 < 5) {
      "Did you wait to longw?"
        waitedToLong2: boolean = true

      "Did you wait to longe?"
        waitedToLong3: boolean = true
    }
}